// SPDX-License-Identifier: Apache-2.0

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Open Program · Admin Console · DataBounty",
};

export default function OpenProgramLayout({ children }: { children: React.ReactNode }) {
  return children;
}
